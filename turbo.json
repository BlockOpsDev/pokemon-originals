{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [
    "$WORKER_KV_KEY",
    "$WORKER_ACCOUNT_ID",
    "$WORKER_KV_NAMESPACE_ID"
  ],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    },
    "start": {
      "cache": false
    },
    "clean": {
      "cache": false
    }
  }
}
